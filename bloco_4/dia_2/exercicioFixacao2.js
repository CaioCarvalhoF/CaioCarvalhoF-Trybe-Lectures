let cars = ["Saab", "Volvo", "BMW"];
for (let i = 0; i < cars.length; i++) {
  console.log(cars[i])
}


let groceryList = ["Arroz", "Feijão", "Alface", "Melancia"];
for (let i = 0; i < groceryList.length; i++) {
    console.log(groceryList[i])
}

// i é o iterador, que passa sobre os elementos do array, inicialmente sendo 0, i tem que ser menor que o tamanho do array (i < array.lenght) e no fim, para aumentar o i e passar para proxima posiçao, usasse i++ ou seja, incrementa-se 1 ao total, a cada vez que o bloco de codigo é executado. 